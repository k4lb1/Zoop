# Optional: Metered Open Relay TURN (https://www.metered.ca/tools/openrelay/)
# Static credentials from the Metered dashboard (iceServers config). If set, used for all users.
# Copy to .env and fill in; .env is gitignored. Credentials are embedded in the built JS (free tier = low risk).
VITE_METERED_TURN_USERNAME=
VITE_METERED_TURN_CREDENTIAL=
